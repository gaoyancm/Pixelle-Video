# 阶段02-D素材与输出资产修复后独立只读复验报告

## 1. 结论摘要

**最终结论：通过。**

本次以新的独立 Codex 身份，对
`D:\AI-Media-Platform\06-source\pixelle-video`
当前尚未提交的阶段02-D首次实施及必要修复成果完成只读复验。

复验取得以下独立证据：

- 冻结 HEAD、父提交、分支、远程、跟踪关系、ahead/behind、暂存区均符合任务门禁；
- D-01至D-04均由当前生产调用链代码、持久化状态断言、故障注入或确定性交错测试独立证实；
- D-05已将首次实施最终四文件定向结果从错误的`35 passed`更正为
  `37 passed, 12 warnings`，且没有倒填本轮新增测试；
- 首次02-D资产模型、上传下载、引用保护、Worker链路、路径安全、legacy隔离、
  API脱敏和0003迁移均未回归；
- 当前完整离线测试为`297 passed, 12 warnings`；
- 当前测试集合没有skip、xfail、xpass或意外deselected；
- Ruff、`git diff --check`、单一Alembic head及隔离迁移往返均通过；
- 测试前后HEAD、暂存区、迁移哈希和工作树状态一致。

因此，D-01至D-04均已被独立证实，当前成果允许进入“本地提交前审阅”。
这不构成暂存、提交、推送、创建PR或进入02-E的授权。

## 2. 独立性与只读声明

- 本复验未参与阶段02-D首次实施或必要修复。
- 实施报告和必要修复报告仅作为待核声明；结论来自当前真实Git、源代码、
  测试、迁移和仓库外临时资源。
- 未修改、格式化、自动修复、创建、删除、移动或清理任何仓库文件。
- 未联网、未安装依赖，未启动Uvicorn、正式数据库、Worker进程、GPU、
  ComfyUI、Provider或真实媒体任务。
- 未暂存、提交、推送、创建PR、合并、Tag、Release或开始02-E。
- pytest禁用缓存插件并设置`PYTHONDONTWRITEBYTECODE=1`；TEMP/TMP定向到
  仓库外`D:\AI-Media-Platform\.tmp-phase02d-reverify`。
- 本报告是唯一生成的交付文件，位于仓库外。

## 3. 冻结基线与Git状态

| 项目 | 独立核验结果 |
| --- | --- |
| 仓库 | `D:\AI-Media-Platform\06-source\pixelle-video` |
| HEAD | `9ff23ffa262681df1df6ddc94d545cffede78677` |
| 父提交 | `745a900fd1ee21e25c88205d72184c3750503171` |
| 分支 | `phase2-persistent-media-jobs` |
| 跟踪分支 | `origin/phase2-persistent-media-jobs` |
| ahead/behind | `0/0` |
| origin | `https://github.com/gaoyancm/Pixelle-Video.git` |
| upstream | `https://github.com/ATH-MaaS/Pixelle-Video.git` |
| 暂存区 | 空；`git diff --cached --name-status`无输出 |
| 已跟踪修改 | 11个 |
| 未跟踪文件 | 19个：12个实现/测试/迁移，7个阶段管理文档 |
| 删除/重命名/模式变化/二进制 | 未发现 |
| 新本地提交 | 无 |

`.pytest_cache`存在既知权限警告；本次未读取、修改或清理该目录，且所有pytest
命令均使用`-p no:cacheprovider`。

### 3.1 迁移冻结证据

| 文件 | 相对HEAD差异 | SHA-256 |
| --- | --- | --- |
| `0001_create_media_jobs.py` | 无 | `C99E5F06E95906A4DC467914B675AE1658355B135E764CF596A29DECF3BB3F8B` |
| `0002_add_media_job_termination.py` | 无 | `66AF58477BFB9C27FDD7E991320536D767B48AE740F24E1A7655B6CA1391DA4F` |
| `0003_add_media_assets.py` | 唯一新增迁移；必要修复轮未改 | `6D6EA9BD12811F38D92D45A99C981048D43DA488087E7B6BFAC8D5D39E32ACD0` |

三个哈希与必要修复报告记录一致。未发现0004或其他迁移。

## 4. 逐文件差异审计

### 4.1 已跟踪修改

| 文件 | 行数统计（+/-） | 归属与判断 |
| --- | ---: | --- |
| `api/app.py` | `+2/-0` | 首次02-D资产路由装配；范围内 |
| `api/dependencies.py` | `+29/-2` | AssetService依赖；范围内 |
| `api/routers/__init__.py` | `+2/-0` | 资产路由导出；范围内 |
| `api/routers/files.py` | `+49/-51` | 旧files路径安全；范围内 |
| `api/routers/media_jobs.py` | `+3/-0` | 任务资产入口；范围内 |
| `api/services/media_jobs.py` | `+17/-2` | UUID资产校验及关系创建；范围内 |
| `pixelle_video/config/schema.py` | `+8/-1` | Asset Store配置；范围内 |
| `pixelle_video/media_jobs/executor.py` | `+117/-17` | 首次链路及D-01/D-02/D-03；修复7文件之一 |
| `pixelle_video/media_jobs/repository.py` | `+92/-3` | 事务关系及D-04竞争约束；修复7文件之一 |
| `pixelle_video/media_jobs/worker_cli.py` | `+23/-8` | 生产一致装配；修复7文件之一 |
| `tests/test_media_job_migrations.py` | `+50/-1` | 0003升降级；范围内 |

合计：`392 insertions, 85 deletions`。这些统计只覆盖Git已跟踪文件。

### 4.2 未跟踪实现、测试和迁移

| 文件 | 当前行数 | 判断 |
| --- | ---: | --- |
| `api/routers/media_assets.py` | 161 | 首次02-D资产API |
| `api/schemas/media_assets.py` | 37 | 首次02-D响应模型 |
| `migrations/versions/0003_add_media_assets.py` | 123 | 唯一新增迁移 |
| `pixelle_video/media_assets/__init__.py` | 35 | 首次02-D模块 |
| `pixelle_video/media_assets/contracts.py` | 96 | 资产合同 |
| `pixelle_video/media_assets/models.py` | 87 | 资产及关系模型 |
| `pixelle_video/media_assets/repository.py` | 389 | 首次实现及D-03/D-04；修复7文件之一 |
| `pixelle_video/media_assets/service.py` | 264 | 首次实现及D-02/D-04；修复7文件之一 |
| `pixelle_video/media_assets/store.py` | 136 | 本地Store安全 |
| `tests/test_files_router_security.py` | 60 | 旧files安全回归 |
| `tests/test_media_assets.py` | 1090 | 首次20项加本轮27项；修复7文件之一 |
| `tests/test_media_assets_api.py` | 173 | 资产API回归 |

### 4.3 未跟踪阶段管理文档

当前有7份02-D管理文档：严格任务契约、只读技术勘察任务、只读技术勘察报告、
实施报告、独立只读验收任务、必要修复任务、修复后独立只读复验任务。
其中实施报告是修复7文件之一，差异仅为D-05计数更正。

必要修复报告所称7个修复文件与真实工作树逐项对应：

1. `executor.py`
2. `worker_cli.py`
3. `media_jobs/repository.py`
4. `media_assets/repository.py`
5. `media_assets/service.py`
6. `tests/test_media_assets.py`
7. `阶段02-D素材与输出资产实施报告.md`

未发现列表外的必要修复轮运行时代码或测试变化。未发现工作流JSON、Provider、
旧UI/pipeline、依赖或锁文件、Docker、CI、部署、AWS、历史迁移或02-E变化。

## 5. D-01至D-05追踪矩阵

| 编号 | 独立代码证据 | 独立测试证据 | 结论 |
| --- | --- | --- | --- |
| D-01 | `worker_cli._build_executor`构造真实生产依赖图并装配AssetService；Executor先查询持久化输入关系，无关系时才走受限legacy resolver；新API事务创建UUID关系 | 生产装配、关系优先、合法legacy、重试、UUID/伪UUID/绝对路径/drive/UNC/穿越/混合分隔符/符号链接矩阵 | 通过 |
| D-02 | 输出组登记和关系提交后标记`registered`；终态Lease/CAS失败只清理未登记对象；恢复先验证关系、状态、对象和兼容投影 | 终态租约丢失、完整恢复、对象缺失、投影不一致、重复观察 | 通过 |
| D-03 | commit异常后以新session独立查询资产、关系和任务投影，分类`not_committed`、`committed`、`unknown`；仅not_committed补偿 | stage/关系/投影/flush/commit故障、已提交、确定未提交、查询不确定、部分持久化、多输出组 | 通过 |
| D-04 | 条件更新先把资产置为`disabled`取得清理权；新增任务、重试、输出登记均要求`available`；最终删除再次检查状态和无引用 | 既有引用、单一赢家、清理与新增引用确定性交错、文件删除失败、数据库最终删除失败、幂等重试 | 通过 |
| D-05 | 实施报告只更正最终四文件定向计数及算术说明 | 当前文档diff及当前64项四文件组复现 | 通过 |

## 6. D-01生产装配与legacy矩阵

| 场景 | 持久化关系 | 预期/实际 |
| --- | --- | --- |
| 新资产任务 | 有input关系 | 按关系解析UUID资产；不走legacy；通过 |
| 合法历史basename路径任务 | 无关系，值符合历史受控格式 | 进入只读legacy resolver；通过 |
| 历史重试任务 | 重试复制历史输入值且无资产关系 | 遵守同一受控legacy规则；通过 |
| 同时存在关系和legacy投影 | 有关系 | 关系优先；legacy值不生效；通过 |
| UUID或伪UUID但无关系 | 无 | 拒绝legacy；通过 |
| `..`、绝对路径、Windows drive/UNC、反斜线或混合分隔符 | 无 | 拒绝；通过 |
| 根外符号链接 | 无 | `resolve`后边界检查拒绝；通过 |
| 新建任务API提交路径型`asset_id` | 不创建 | UUID校验拒绝；由API回归通过 |

定向命令结果：`11 passed, 36 deselected`。36项deselected由显式`-k`
筛选产生，属于预期定向选择；完整组和全量测试均无deselected。

## 7. D-02/D-03失败点—持久化—对象—任务矩阵

| 故障点 | 持久化分类/结果 | Store对象 | 任务结果 | 结论 |
| --- | --- | --- | --- | --- |
| 资产stage失败 | `not_committed`，无资产/关系/投影 | 删除本轮对象 | FAILED，不成功 | 通过 |
| 关系stage失败 | `not_committed`，事务回滚 | 删除本轮对象 | FAILED | 通过 |
| 兼容投影失败 | `not_committed` | 删除本轮对象 | FAILED | 通过 |
| flush失败 | `not_committed` | 删除本轮对象 | FAILED | 通过 |
| commit抛错且独立查询确认完整事实 | `committed` | 保留 | 不猜测失败；可恢复 | 通过 |
| commit失败且独立查询确认无事实 | `not_committed` | 删除本轮对象 | FAILED | 通过 |
| 独立查询本身失败 | `unknown`保守路径 | 保留 | 不误报成功，等待对账/重试 | 通过 |
| 仅部分资产/关系事实可见 | `unknown` | 保留 | 不猜测删除 | 通过 |
| 登记已提交后成功终态Lease/CAS丢失 | 已提交资产、关系、投影保留 | 正式对象保留 | 不错误标记成功 | 通过 |
| Worker再次观察完整已提交输出 | 验证关系、available状态、对象及投影 | 保留且不重复创建 | 完成成功终态 | 通过 |
| 再观察缺对象或不一致投影 | 关系事实保留并识别不完整 | 不覆盖错误对象 | FAILED/OUTPUT_MISSING | 通过 |

D-02/D-03定向结果：`12 passed, 35 deselected`；deselected同样是显式定向
选择的预期结果。四文件组与全量组无deselected。

## 8. D-04并发交错与恢复矩阵

| 场景 | 状态与竞争结果 | 文件/数据库结果 | 结论 |
| --- | --- | --- | --- |
| 已有引用时申请清理权 | 条件更新受无引用谓词阻止 | 不删文件、不删记录 | 通过 |
| 两个清理者并发 | 只有一个从可用态取得`disabled` | 单一赢家；重复调用安全 | 通过 |
| 清理claim与新增关系竞争 | 二者不能同时成功 | 若claim成功，后续引用被available谓词阻止 | 通过 |
| retry复制输入与清理竞争 | retry要求同一资产仍available | 与清理状态互斥 | 通过 |
| 输出登记与清理竞争 | 输出资产必须available | 与清理状态互斥 | 通过 |
| 文件删除失败 | 保持`disabled`、不可下载/引用 | 对象可仍在；一分钟后可重试 | 通过 |
| 文件已删、数据库最终删除失败 | 保持`disabled`，不恢复available | 对象缺失；后续重试收敛删除记录 | 通过 |
| 记录已删除后重复清理 | 查无记录直接返回 | 幂等且安全 | 通过 |
| 普通软删除 | 引用保护仍有效 | 不破坏既有软删除语义 | 通过 |

并发/claim定向结果`3 passed, 44 deselected`；文件与数据库失败专项
`1 passed`。关键竞态由条件更新行数和确定性交错测试证明，不是仅凭事务外观推断。

## 9. 原02-D能力、API与安全回归

| 能力 | 证据 | 结果 |
| --- | --- | --- |
| `media_assets`/`media_job_assets`模型、外键、唯一约束和索引 | 模型、0003、迁移测试 | 通过 |
| 上传即注册、UUID、幂等、冲突补偿、内容/MIME/大小校验 | Service/API/并发测试 | 通过 |
| 受控查询、下载、缺文件标记、软删除和引用保护 | Service/API测试 | 通过 |
| Worker输入资产和输出登记链路 | 生产装配及Executor测试 | 通过 |
| 新任务仅接受真实资产UUID | API及Service测试 | 通过 |
| legacy只读隔离 | D-01矩阵 | 通过 |
| 对账只报告，不做破坏性修复 | reconciliation测试 | 通过 |
| `/api/files`穿越、编码、Windows/UNC、符号链接及错误脱敏 | 7项路由安全测试 | 通过 |
| API不泄露`object_key`、Store根或绝对路径 | schema/API测试 | 通过 |
| 02-A/02-B/02-C公开HTTP语义 | 297项全量回归 | 通过 |

未运行真实PostgreSQL、多节点负载、junction专项系统矩阵、Uvicorn、真实Worker、
GPU、ComfyUI、Provider、真实媒体任务、云对象存储、鉴权或部署；这些均为任务
明确排除项，不作为本次通过依据。

## 10. 迁移往返

- `alembic heads`：`0003_add_media_assets (head)`，单一head；
- `tests/test_media_job_migrations.py`：`6 passed`；
- 6项测试在仓库外临时SQLite资源上覆盖空库upgrade head、既有0002升级、
  `0002 → 0003 → 0002 → 0003`往返以及downgrade历史结构保护；
- 0001、0002无工作树diff，0003哈希与修复前证据一致；
- 未连接或修改正式数据库。

## 11. 测试完整性与`270 + 27 = 297`

独立核验：

- 首次独立验收报告记录修复前完整基线为`270 passed, 12 warnings`；
- 修复前`tests/test_media_assets.py`为20项；当前收集为47项，净新增27项；
- 当前四文件02-D组为：
  - `test_media_job_migrations.py`：6项；
  - `test_files_router_security.py`：7项（3个函数经参数化收集）；
  - `test_media_assets.py`：47项（29个函数经参数化收集）；
  - `test_media_assets_api.py`：4项；
  - 合计64项；
- 当前全仓收集并执行`297 passed, 12 warnings`；
- 算术成立：`270 + 27 = 297`；
- 冻结点原238项与首次02-D净新增32项的历史算术仍为
  `238 + 32 = 270`；
- 首次最终四文件组37项含冻结点已有5项迁移测试，因此与净新增32不冲突；
- Git显示唯一修改的既有测试文件仍是迁移测试；未发现已跟踪测试删除或重命名；
- 全测试目录搜索未发现`pytest.skip`、`pytest.xfail`、对应marker、
  `unittest.skip`或`assert True`恒真断言；
- 完整执行结果没有failed、error、skipped、xfailed、xpassed或deselected。

定向`-k`命令显示的deselected仅为显式选择结果，已逐项如实记录，不能与完整
回归中的意外deselected混同。

## 12. 命令与准确结果

所有pytest命令均设置UTF-8、`PYTHONDONTWRITEBYTECODE=1`、仓库外TEMP/TMP，
并使用`-p no:cacheprovider`。

| 命令（环境前缀略） | 退出码 | 结果 |
| --- | ---: | --- |
| `python -m pytest -p no:cacheprovider --collect-only -q tests/test_media_assets.py` | 0 | 47 tests collected |
| `python -m pytest -p no:cacheprovider -q tests/test_media_assets.py -k "production or relation_wins_over_legacy_projection"` | 0 | 11 passed, 36 expected deselected |
| D-02/D-03显式`-k`定向命令 | 0 | 12 passed, 35 expected deselected |
| D-04 claim/竞争显式`-k`定向命令 | 0 | 3 passed, 44 expected deselected |
| `pytest ...::test_cleanup_file_failure_and_database_failure_remain_unavailable` | 0 | 1 passed |
| `pytest ... test_media_job_migrations.py test_files_router_security.py test_media_assets.py test_media_assets_api.py` | 0 | 64 passed, 12 warnings |
| `python -m pytest -p no:cacheprovider -q` | 0 | 297 passed, 12 warnings, 29.65s |
| `ruff check --no-cache --ignore E402 <全部02-D新增/修改Python文件>` | 0 | All checks passed! |
| `python -m alembic heads` | 0 | 单一`0003_add_media_assets (head)` |
| `pytest ... tests/test_media_job_migrations.py` | 0 | 6 passed |
| `git diff --check` | 0 | 无输出，通过 |
| `git diff --cached --check` | 0 | 无输出，通过 |

12条warning均为既有Pydantic V2弃用提示，与修复前口径一致。

## 13. 测试前后Git对比

| 项目 | 测试前 | 测试后 |
| --- | --- | --- |
| HEAD | `9ff23ffa262681df1df6ddc94d545cffede78677` | 相同 |
| 分支/跟踪 | `phase2-persistent-media-jobs` / origin同名 | 相同 |
| ahead/behind | `0/0` | 相同 |
| 暂存区 | 空 | 空 |
| 已跟踪修改 | 同一11个文件 | 同一11个文件 |
| 未跟踪仓库文件 | 同一19个文件 | 同一19个文件 |
| 0001/0002 diff | 无 | 无 |
| 0001/0002/0003哈希 | 冻结值 | 相同 |
| 新删除/重命名/模式/二进制变化 | 无 | 无 |

测试未在仓库中产生新的可见或无法解释变化。

## 14. 新发现问题

未发现影响代码正确性、数据一致性、安全、兼容、迁移或提交范围的新缺陷。

非缺陷说明：

- `.pytest_cache`权限警告为已知环境状态，已通过禁用pytest缓存规避；
- Pydantic 12条弃用warning为既有历史告警；
- 定向命令的deselected是显式`-k`选择，完整回归不存在deselected。

## 15. 四选一最终结论与后续建议

**最终结论：通过。**

D-01至D-04全部以独立代码、持久化状态、故障注入或并发交错证据通过；
D-05事实口径已正确更正；原02-D能力、迁移、安全和297项完整回归通过；
没有未解释差异。

**建议：允许进入“本地提交前审阅”。**

本报告不授权本地提交。仓库应继续保持HEAD不变、暂存区为空、成果未提交；
不得自行推送、创建PR或开始02-E。
