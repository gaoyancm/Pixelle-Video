# AI Media Platform 阶段02-C修复后独立只读复验报告

## 1. 最终结论

**通过，可进入阶段02-C本地提交准备。**

本次复验独立读取真实工作树和真实diff，并独立重跑测试，没有直接采信《阶段02-C必要修复报告》的结论或测试声明。F-01、F-02、F-03、F-04均已关闭；未发现P0、P1或P2问题。实施方所称“02-A/02-B七文件121 passed”不能按本报告采用的自然七文件集合原样复现，实际为`124 passed`；但完整套件仍独立复现为`238 passed`，且相关测试无失败、跳过或xfail，因此该计数差异记为P3报告口径问题，不影响代码、安全或核心语义。

本结论只表示可以由项目负责人另行授权“阶段02-C本地提交准备”。本次未暂存、未提交、未推送、未创建PR或合并，也没有开始02-D。

## 2. Git开始状态

| 项目 | 独立核验结果 |
|---|---|
| 仓库绝对路径 | `D:\AI-Media-Platform\06-source\pixelle-video` |
| 分支 | `phase2-persistent-media-jobs` |
| 完整HEAD | `745a900fd1ee21e25c88205d72184c3750503171` |
| 跟踪分支 | `origin/phase2-persistent-media-jobs` |
| ahead / behind | `0 / 0` |
| origin | `https://github.com/gaoyancm/Pixelle-Video.git` |
| upstream | `https://github.com/ATH-MaaS/Pixelle-Video.git` |
| 暂存 | 无 |
| 删除 | 无 |
| 历史迁移0001 | `git diff --exit-code HEAD -- migrations/versions/0001_create_media_jobs.py`为0，无diff |

开始时为8个已跟踪修改和12个未跟踪文件；集合可由原02-C的16个实现文件以及独立验收、必要修复契约、必要修复报告、本次复验任务四份文档完整解释，未触发硬停止条件。

## 3. 完整文件差异与范围核验

### 3.1 已跟踪修改（8）

- `api/app.py`
- `api/dependencies.py`
- `api/routers/__init__.py`
- `pixelle_video/media_jobs/contracts.py`
- `pixelle_video/media_jobs/models.py`
- `pixelle_video/media_jobs/repository.py`
- `tests/test_media_job_migrations.py`
- `tests/test_media_job_repository.py`

### 3.2 开始时未跟踪（12）

- `api/routers/media_jobs.py`
- `api/schemas/media_jobs.py`
- `api/services/__init__.py`
- `api/services/media_jobs.py`
- `migrations/versions/0002_add_media_job_retry_lineage.py`
- `tests/test_media_jobs_api.py`
- `docs/zh/development/persistent-media-jobs-phase2c-api.md`
- `docs/zh/development/阶段02-C持久化任务API实施报告.md`
- `docs/zh/development/阶段02-C独立只读验收报告.md`
- `docs/zh/development/阶段02-C必要修复严格任务契约.md`
- `docs/zh/development/阶段02-C必要修复报告.md`
- `docs/zh/development/阶段02-C修复后独立只读复验任务.md`

未发现Worker、Executor、ComfyUI Adapter、四个工作流JSON、Streamlit、依赖或锁文件的diff；无新增依赖、认证、上传、下载、Webhook、SSE、WebSocket、管理端点或额外迁移。历史迁移`0001`未修改。

## 4. F-01：单值asset_id

**结论：关闭。**

- `api/schemas/media_jobs.py::MediaJobRequest`仅公开可选单值`asset_id`，`extra="forbid"`拒绝`asset_ids`；`_ASSET_ID`限制为1至128个批准安全字符。
- `MediaJobRequest.parameters_must_match_workflow`从`WORKFLOW_SPECS`读取`requires_image`：两个I2V必须恰有一个资产，两个T2V拒绝资产。
- `api/services/media_jobs.py::MediaJobApplicationService.create`把单值转换为一个`MediaInputAsset(asset_id=..., role="input_image")`，没有改变02-A/02-B内部列表合同。
- `tests/test_media_jobs_api.py::test_i2v_accepts_single_asset_and_converts_to_internal_list`真实查询临时SQLite行；`test_asset_contract_rejects_invalid_external_shapes_without_rows`覆盖缺失、复数字段、T2V资产、空值、空白、路径字符和超长值，并确认Repository行数为0。
- 独立定向重跑覆盖合法I2V、`asset_ids`非法、T2V带资产、OpenAPI只公开`asset_id`及稳定幂等。

## 5. F-02：公开参数强校验及零数据库副作用

**结论：关闭。**

- `api/schemas/media_jobs.py::MediaJobParameters`集中定义严格类型及边界：`prompt` 1..4096且非空白，`negative_prompt` 0..4096，宽高1..16384，帧数1..10000，seed 0..`2^64-1`，steps 1..1000，cfg 0..100且有限。
- 数字字段使用Pydantic严格类型，布尔、字符串、对象和数组不能冒充数字；字符串字段同样严格。`cfg_must_be_finite`拒绝NaN/Infinity。
- 工作流唯一名称及适用性来自`WORKFLOW_SPECS`和`parameter_targets`，没有第二套四工作流白名单；未知参数和不适用参数均拒绝。
- 序列化参数体限制32 KiB。`prompt`和`negative_prompt`的多字节组合越界用例返回422。
- `tests/test_media_jobs_api.py::test_invalid_public_parameters_are_rejected_without_side_effects`覆盖首次复现的四个非法值及类型/上下界/未知/不适用矩阵，每例后查询真实临时Repository并确认任务数为0。不存在独立幂等表；幂等唯一性存于`media_jobs`行，因此0行也证明无幂等持久化副作用。
- 合法最小值和最大值由`test_public_parameter_valid_boundaries`覆盖。

## 6. F-03：固定脱敏500

**结论：关闭。**

- `api/routers/media_jobs.py::MediaJobRoute.get_route_handler`在已知404、409、422、503映射之后捕获未知异常，仅记录固定日志消息，并返回严格固定JSON：`{"error":{"code":"internal_error","message":"An internal error occurred."}}`。
- 既有响应级测试使用`raise_app_exceptions=False`注入含伪秘密和本机路径的异常。
- 本次另行通过内联只读探针向两个不同路径注入不同异常类型：`GET /api/media/jobs/{job_id}`及`GET /api/media/jobs?limit=1&offset=0`均为500、`application/json`、严格固定结构，响应中没有异常类名、Token、DSN、路径或原始幂等键。
- 已知503由`MediaJobsDisabledError`独立测试为固定`service_unavailable`且不泄露DSN；404、409和422定向测试保持原映射，没有被宽泛异常改写为500。
- 该异常映射限定于新路由的`MediaJobRoute`，未改变旧API全局异常行为。

## 7. F-04：关键覆盖有效性

**结论：关闭。**

### 7.1 三页分页

`test_pagination_is_stable_complete_filtered_and_does_not_leak_extra_row`使用真实API和临时Repository创建5行、统一`created_at`，按`created_at DESC, job_id DESC`跨三页验证：页间无重复漏项，前两页`has_more=true`，末页`false`，额外`limit+1`行不泄露，无`total`，状态过滤与分页组合正确。Repository的`list_jobs`真实使用稳定双字段排序。

### 7.2 八路并发重试

`test_api_concurrent_retry_and_operation_source_scopes_are_isolated`对同一失败原任务发起8路API并发重试，独立重跑得到一个子任务、一个201和七个200，无500或重复；同原始键在创建/重试、不同原任务间隔离。Repository测试同时覆盖原子创建、血缘、递增`retry_count`、新submission token、原任务不变及`submission_unknown`不可重试。`MediaJobRepository.create_retry_job`在单一`session.begin()`短事务中读取资格并插入子任务。

### 7.3 服务不可用、OpenAPI和架构边界

- 服务不可用固定为`503 service_unavailable`，不返回原始原因。
- 独立OpenAPI探针确认5个操作，公开`asset_id`而无`asset_ids`，且公开路径/Schema不含submission token、request hash、租约、prompt ID或路径等内部字段。
- AST测试确认Router不导入旧`TaskManager`、Worker、Executor或ComfyUI；导入行为测试把数据库连接替换为禁止调用后仍可导入并得到5条route。
- `api.app`路径检查确认旧`/api/tasks/{task_id}`、`/api/video/generate/sync`和`/api/video/generate/async`仍注册。
- 边界证据包含AST、导入行为、真实Schema、真实Repository和真实事务，不只依赖字符串扫描或过度Mock。

## 8. 原02-C能力、幂等、取消、重试和迁移

- 新API恰好5个批准操作；`WORKFLOW_SPECS`仍只提供4个批准工作流。
- 创建与重试使用操作及来源作用域哈希后的幂等键；数据库唯一约束仍保证唯一性。
- 取消只通过Repository CAS写入`cancel_requested_at`，不调用远端中断；重复取消幂等，终态返回409。
- 重试创建新任务并保留原任务；`retry_of_job_id`只由服务端设置，外部输入因`extra="forbid"`被拒绝；`submission_unknown`不可重试。
- `0002_add_media_job_retry_lineage.py`仅新增nullable `retry_of_job_id VARCHAR(36)`和普通非唯一索引；`down_revision`为`0001_create_media_jobs`。迁移测试在临时SQLite完成升级、降至0001、再升级及ORM一致性。
- `python -m alembic heads`只输出`0002_add_media_job_retry_lineage (head)`；未对任何真实数据库运行迁移。

## 9. 独立测试和检查

统一环境：使用仓库既有`.venv`，`TEMP/TMP=D:\AI-Media-Platform\.phase2c-reverification-tmp`，`PYTHONIOENCODING=utf-8`、`PYTHONUTF8=1`、`PYTHONDONTWRITEBYTECODE=1`，pytest均加`-p no:cacheprovider`。没有安装或升级依赖。

| 检查 | 收集/选择 | 结果 | warnings | 耗时 |
|---|---:|---|---:|---:|
| `pytest tests/test_media_jobs_api.py -k "asset or parameter or pagination or concurrent_retry or unknown_exception or service_unavailable or openapi or boundaries or side_effect" -ra` | 85收集，58选择，27 deselected | 58 passed | 12 | 4.44s |
| `pytest tests/test_media_jobs_api.py -ra` | 85 | 85 passed | 12 | 4.44s |
| `pytest tests/test_media_job_repository.py -ra` | 27 | 27 passed | 0 | 2.22s |
| `pytest tests/test_media_job_migrations.py -ra` | 5 | 5 passed | 0 | 1.61s |
| 02-A/02-B自然对应七文件：config、executor、migrations、recovery、repository、state_machine、worker | 124 | 124 passed | 0 | 6.17s |
| `pytest -ra`完整离线套件 | 238 | 238 passed | 12 | 10.52s |
| `ruff check --no-cache --ignore E402`全部14个02-C Python变更文件 | — | All checks passed | — | <1s |
| `git diff --check` | — | 通过 | — | <1s |
| `python -m alembic heads` | — | 单一head | — | <1s |
| 双路径脱敏500内联探针 | 2路径 | 2通过 | 0 | <2s |
| 独立OpenAPI内联探针 | 5操作 | 通过 | 0 | <2s |

所有pytest有效执行均为0 failed、0 skipped、0 xfail。12条warning均来自既有旧API Pydantic弃用提示。

实施方关于API 85、Repository 27、迁移5、完整238、Ruff及diff检查的声明均独立复现。其“02-A/02-B七文件121”未给出完整文件命令；按上述最直接的七个持久化任务相关文件实际为124。该差异不代表测试减少或回归失败。

## 10. 敏感信息、错误响应和公开面

正常响应由显式`MediaJobResponse`投影，不直接序列化ORM；输出移除`relative_path`。错误响应不包含异常文本、堆栈、路径、DSN、Token、原始幂等键或下游响应。OpenAPI和5个操作未公开`asset_ids`、provider、node、请求哈希、submission token、prompt ID、租约或版本字段。未发现日志主动记录完整请求体、原始幂等键或异常文本。

## 11. 问题清单

| 编号 | 等级 | 结论 | 证据与建议 |
|---|---|---|---|
| R-01 | P3 | 实施方“02-A/02-B七文件121 passed”缺少可重复的完整文件命令；自然七文件集合实际为124 passed | 代码和完整238项均通过。提交前可在实施报告中补充原命令或更正计数；不构成P0/P1/P2，也不阻断本地提交准备。 |

无P0、P1或P2问题。

## 12. 范围外、未运行项目

按任务边界未运行真实PostgreSQL、生产多节点、GPU、ComfyUI、Uvicorn、外部Worker、真实媒体任务、公网、负载/长稳、认证/授权/渗透、AWS或Docker部署。SQLite结果不外推为这些范围已通过。

## 13. Git结束状态与完整性

结束复核确认：仓库仍为`D:\AI-Media-Platform\06-source\pixelle-video`，分支仍为`phase2-persistent-media-jobs`，完整HEAD仍为`745a900fd1ee21e25c88205d72184c3750503171`，跟踪仍为`origin/phase2-persistent-media-jobs`且ahead/behind仍为`0/0`。除本报告外，8个已跟踪修改和12个原有未跟踪文件集合与开始一致；无暂存、无删除，`git diff --check`通过，`0001`仍无diff。测试过程没有改写其他受版本控制文件。

本次唯一新建的项目文件是：

`docs/zh/development/阶段02-C修复后独立只读复验报告.md`

## 14. 停止声明

本次只读复验没有修复或格式化代码，没有暂存、提交、推送、创建PR或合并，没有安装依赖，没有运行真实数据库迁移，没有启动GPU、ComfyUI、Uvicorn、外部Worker或真实媒体任务，也没有开始02-D。

报告完成后立即停止，等待项目负责人下一步授权。
