# AI Media Platform—阶段02代表性端到端最终独立验收报告

> 验收日期：2026-07-29（America/Los_Angeles）
> 仓库：`D:\AI-Media-Platform\06-source\pixelle-video`
> 冻结提交：`35a6705f8f309d7af8853ea201fa991a3c03f730`
> 实施证据：`D:\AI-Media-Platform\90-exports\phase02-e2e-20260729-194130`
> 独立验收证据：`D:\AI-Media-Platform\90-exports\phase02-final-independent-acceptance-20260729-201036`

## 1. 最终结论

阶段02代表性端到端最终独立验收通过，已具备创建阶段02最终本地提交并推送个人Fork的条件；无需再次执行真实GPU任务。

本次只做独立验收和自主收口，没有启动API、Worker或真实Provider，没有访问
A800、4090、ComfyUI或受保护端口，没有创建、取消、重试或修改任务，没有提交、
推送或创建PR。

## 2. 自主小修正

没有对既有生产代码、测试、实施报告或既有证据进行自主小修正。

本任务只新增本报告，以及仓库外的独立验收日志和索引。最终报告是任务明确授权的
第三份未跟踪文档，不属于异常漂移。

## 3. Git开始现场

开始现场符合冻结要求：

- 分支为`phase2-persistent-media-jobs`；
- HEAD与`origin/phase2-persistent-media-jobs`均为
  `35a6705f8f309d7af8853ea201fa991a3c03f730`；
- behind/ahead为`0/0`；
- 暂存区为空；
- 已跟踪修改精确为任务指定的9份Python文件；
- 未跟踪文件精确为既有严格实施任务和实施报告两份文档；
- `git diff --check`通过；
- 没有本地提交、推送或PR；
- 可访问的`Get-Process`检查未发现Python、Pythonw或Uvicorn进程；
- 18080没有监听。

系统拒绝读取`Win32_Process.CommandLine`和`tasklist`，因此进程现场同时以
`Get-Process`、18080监听结果和既有清理证据交叉确认。该权限限制没有遮蔽任何
已发现的相关进程或端口。

## 4. 九份diff独立审查

相对冻结SHA逐份完整审查9份diff，实际规模为369行新增、22行删除。

### 4.1 五份生产代码

- `api/dependencies.py`：生产依赖从同一`ConfigManager`取得任务配置与ComfyUI
  节点配置，并把唯一共享选择器注入Application Service。
- `api/services/media_jobs.py`：公开请求解析为注册工作流后，先选择非空节点，
  再构造包含`node_id`的内部`MediaJobCreate`；无节点时在任何Repository插入前
  转为固定脱敏503。
- `pixelle_video/services/comfyui_adapter.py`：提取唯一纯函数
  `select_comfyui_node`，Adapter和API装配共用同一“第一个启用且支持工作流”算法，
  没有第二套节点选择逻辑。
- `pixelle_video/media_jobs/state_machine.py`：仅增加`queued -> failed`，使
  Provider提交前的持久化节点校验失败可安全终止；没有增加重试、提交或公开合同。
- `pixelle_video/media_jobs/repository.py`：在同一事务中先校验并收集输入关系，
  再插入/flush父任务，最后插入/flush关系，修复SQLite外键顺序；公开合同、ORM和
  迁移不变。

### 4.2 四份测试

- `tests/test_media_jobs_api.py`：覆盖公开`node_id`拒绝、OpenAPI请求/响应不暴露、
  实际响应不暴露、创建前持久化节点、幂等重放、retry继承、共享选择算法及无节点
  503零任务。
- `tests/test_media_job_executor.py`：覆盖公开API到真实Worker的mock单提交交接，
  以及节点不匹配时Provider零调用、failed安全错误、提交标记保持空和lease清理。
- `tests/test_media_assets_api.py`：使用生产SQLite引擎开启外键，直接核验父任务先于
  输入关系、外键完整性和幂等重放不重复写入。
- `tests/test_media_job_repository.py`：最小导入资产关系模型，使测试元数据包含真实
  外键表。

### 4.3 十二项语义结论

独立审查确认：

1. 公开请求不能指定`node_id`，公开Schema和实际响应均不读取或暴露它；
2. Application Service在请求哈希和Repository插入前选择并保存非空节点；
3. API和Adapter共用唯一纯节点选择器；
4. 无节点返回固定脱敏503，任务插入0行，Provider调用0次；
5. Worker使用持久化节点，不静默切换物理节点；
6. 节点不匹配时Provider调用0次，任务安全进入failed；
7. failed路径保存非空脱敏错误并清空全部lease字段；
8. 该失败路径的`submit_started_at`和`comfyui_prompt_id`保持空；
9. 幂等重放保留同一非空节点；
10. retry继承父任务节点；
11. Repository外键顺序修复不改变公开合同、ORM或迁移；
12. 没有发现第二套选择算法、隐藏范围扩大或第三项生产缺陷。

## 5. 三个最终验收缺口

三个缺口真实闭合：

- POST显式传入`node_id`由`extra="forbid"`返回422；OpenAPI请求、响应组件以及
  实际响应均不包含`node_id`；
- 无可用节点时固定返回脱敏503，直接断言数据库0任务且Provider调用0次；
- 节点不匹配时Provider调用0次，错误非空且包含安全语义，不泄露持久化节点ID、
  URL、Authorization、API key或凭据；提交标记为空且lease字段全部清空。

精确补强选择重新收集并执行为14项，结果`14 passed`。

## 6. 最终离线验证

所有命令均使用仓库`.venv`、`PYTHONDONTWRITEBYTECODE=1`、
`-p no:cacheprovider`以及仓库外独立`TEMP/TMP`和`--basetemp`。

| 验证 | 结果 |
| --- | --- |
| 三类验收缺口精确测试 | `14 passed` |
| node_id相关API、Repository、Worker、Executor | `162 passed` |
| 既有21项离线门禁 | `21 passed` |
| 全量离线收集 | `440 tests collected` |
| 完整离线回归 | `440 passed` |
| 9份变更Python文件Ruff | 通过 |
| `git diff --check` | 通过 |
| Alembic heads | 唯一`0003_add_media_assets (head)` |
| 0001—0003相对冻结SHA | 零差异 |

测试集合没有live、GPU或外部服务标记；全部测试使用本地临时目录、mock transport或
静态分析，因此完整回归没有启动生产API、Worker、远程Provider或GPU连接。12条
warning均为既有Pydantic弃用提示，不是当前修改导致的失败。

## 7. A800成功证据独立核验

交叉核对INDEX、JSON、SQLite只读事实和四份进程日志后确认：

- 全新数据库在创建前为0任务、0资产、0关系；
- 公开任务创建调用1次，只产生任务
  `dd08f531-44f9-4b39-bb9e-82ba86319ba9`；
- 创建后、Worker启动前已经持久化`node_id=a800`；
- Worker进程1个，唯一worker_id，初始queued领取1次；
- Worker日志中`POST http://127.0.0.1:18188/prompt`精确出现1次；
- 唯一`prompt_id`为`578ff7e8-7985-49ce-b131-b6507bd9b2e1`；
- submission token在完整history中只匹配该唯一prompt_id；
- 无retry、无第二任务，4090访问0次；
- 状态时间线为`queued -> running -> succeeded`；
- 终态错误为空，lease owner、lease expiry和heartbeat全部为空；
- 最终只有一个available输出资产和一条`generated_video`输出关系；
- 公开任务查询、资产查询和内容下载成功，公开响应不含`node_id`；
- 本轮API与Worker已清理，18080无监听。

对当前成功SQLite执行只读交叉检查，仍为1任务、1资产、1关系、1个prompt_id，
`integrity_check=ok`且`foreign_key_check=[]`。

旧失败任务
`24dd778b-5112-467e-a0f1-8de303fd07e1`仍为queued、`node_id=NULL`、
提交标记和prompt_id为空、既有lease/version事实不变；主数据库和WAL内容没有被
本任务修改。SQLite只读打开WAL数据库可能更新`-shm`同步侧车的访问状态，但没有
改变旧任务、主数据库或WAL业务事实。

## 8. 输出资产与MP4

输出资产：

- `asset_id=a2fd7549-d891-4083-b237-e3cf9701cd8c`；
- 状态`available`，关系角色`generated_video`；
- 大小177,485字节；
- 文件、SQLite及既有公开下载SHA-256一致：
  `809B905B6EDAD372B5E0B2E8CD3D765584AD746F659CDD14BB26CEFCF7A06EBF`。

独立只读媒体检查：

- `ffprobe`识别为MP4容器、H.264 High、512×512、33帧、16fps、2.0625秒；
- 完整`ffmpeg`解码退出码0且无错误；
- 33个解码帧产生33个不同帧哈希；
- 32组相邻帧差异全部非零，差异亮度均值最小1.47989、最大3.9025；
- 因此视频包含连续变化画面，不是静态占位文件。

短样本的商业画质不属于本次阶段02技术可行性阻断条件。

## 9. 范围与结束现场

没有发现范围扩大、第三项生产缺陷、迁移/ORM/公开合同变化、离线回归失败或A800
证据矛盾。结束现场仍保持冻结HEAD/origin和`0/0`，暂存区为空，已跟踪修改仍精确
为原9份文件；未跟踪项为既有两份文档加本报告，没有提交、推送或PR，没有相关
Python进程或18080监听。

## 10. 证据路径

- 实施报告：
  `D:\AI-Media-Platform\06-source\pixelle-video\docs\zh\development\阶段02代表性端到端可行性验证实施报告.md`
- A800最终成功证据：
  `D:\AI-Media-Platform\90-exports\phase02-e2e-20260729-194130`
- 本次独立验收命令、完整日志与媒体检查：
  `D:\AI-Media-Platform\90-exports\phase02-final-independent-acceptance-20260729-201036`
