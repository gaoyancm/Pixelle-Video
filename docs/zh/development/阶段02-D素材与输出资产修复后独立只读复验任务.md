# 阶段02-D素材与输出资产修复后独立只读复验任务

## 第1节　文档性质

本文档是阶段02-D必要修复完成后的独立只读复验任务书。

它只授权新的、未参与02-D实施和必要修复的Codex，对当前未提交工作树进行只读复验，并在仓库外生成：

> 《阶段02-D素材与输出资产修复后独立只读复验报告.md》

本文档不是代码修改、再次修复、本地提交、推送、创建PR或进入阶段02-E的授权。

## 第2节　复验目标

复验必须独立确认：

1. 首次验收发现的D-01至D-04已经在真实运行链路中得到最小、完整且可恢复的修复；
2. D-05测试计数已经按真实历史证据更正；
3. 必要修复没有破坏首次02-D实施已通过的资产、迁移、API、安全与兼容能力；
4. 原有270项测试完整保留，本轮净新增27项，最终297项测试真实有效；
5. 当前差异与必要修复报告一致，没有越界实现或未解释变化；
6. 当前状态仍为未暂存、未提交、未推送。

## 第3节　权威资料与优先级

发生冲突时按以下顺序处理：

1. 本复验任务；
2. 《阶段02-D素材与输出资产必要修复任务.md》；
3. 《阶段02-D素材与输出资产独立只读验收报告.md》；
4. 《阶段02-D素材与输出资产严格任务契约.md》；
5. 《阶段02-D素材与输出资产必要修复报告.md》；
6. 《阶段02-D素材与输出资产实施报告.md》；
7. 《阶段02-D素材与输出资产只读技术勘察报告.md》；
8. 项目总体规划与此前阶段最终报告；
9. 冻结提交和当前工作树中的真实代码、测试与Git证据。

实施方或修复方报告中的“已完成”“已通过”只能作为待核声明，不能代替独立证据。

## 第4节　冻结基线

必须核验：

- 仓库：`D:\AI-Media-Platform\06-source\pixelle-video`
- 冻结起始HEAD：`9ff23ffa262681df1df6ddc94d545cffede78677`
- 当前分支、父提交、远程地址和跟踪关系；
- 冻结HEAD相对上游跟踪分支的ahead/behind；
- 暂存区为空；
- 02-D实施与必要修复仍全部位于未提交工作树；
- `0001`、`0002`、`0003`的状态与授权边界一致；
- 没有必要修复报告未解释的已跟踪或未跟踪代码文件。

不得假定工作树应当干净。当前预期是：冻结HEAD不变，工作树包含尚未提交的02-D首次实施和必要修复成果。

## 第5节　只读边界

允许：

- 读取代码、测试、配置、迁移、报告和Git元数据；
- 执行无外部副作用的离线测试、Ruff、`git diff --check`和测试收集；
- 使用临时目录、临时SQLite数据库或等价隔离资源验证迁移与故障场景；
- 使用测试替身、故障注入和并发测试；
- 在仓库外生成复验报告。

禁止：

- 修改、格式化、自动修复或生成仓库内文件；
- 修改测试来观察结果；
- 启动Uvicorn、正式数据库、Worker、GPU、ComfyUI、Provider或真实媒体任务；
- 联网安装依赖或调用外部服务；
- 暂存、提交、推送、建PR、合并、Tag或Release；
- 顺手修复发现的问题；
- 开始阶段02-E。

如工具会生成缓存、覆盖率文件或其他仓库内副产物，必须改用仓库外临时目录；无法保证只读时不得运行该工具。

## 第6节　开始前硬停止检查

下列任一情况成立，必须停止，不得继续测试：

1. 当前HEAD不是完整冻结值；
2. 分支、远程或跟踪关系与资料无法解释地不一致；
3. 暂存区非空；
4. 已存在新的本地提交；
5. `0001`、`0002`或`0003`被必要修复轮修改；
6. 工作树存在无法归因于首次实施或必要修复的代码变化；
7. 必需资料缺失，导致无法确定D-01至D-04的原始缺陷或验收标准；
8. 只读复验必然需要修改仓库。

停止时只生成阻断说明，不得自行恢复、清理或修改。

## 第7节　Git与差异审计

报告必须给出：

- 完整HEAD、父提交、分支、远程、跟踪分支及ahead/behind；
- 暂存区状态；
- `git status --short`或等价完整状态；
- 从冻结HEAD到当前工作树的逐文件差异表；
- 首次实施文件、必要修复文件、报告文件和未跟踪管理文档的分类；
- 新增、修改、删除文件及行数统计；
- 必要修复报告所称7个修复文件与真实差异的逐项对应；
- 是否存在重命名、模式变化、二进制文件或隐藏的阶段外修改。

不得只写“与报告一致”。

## 第8节　D-01生产装配与legacy回退复验

必须独立验证：

1. 生产Worker真实装配`AssetService`时，合法的02-A至02-C历史路径型任务仍可进入受控legacy读取回退；
2. 分流依据首先是持久化任务—输入资产关系，而不是仅看参数格式或是否注入`AssetService`；
3. 存在任务资产关系的新任务必须按资产UUID读取，不得回退到路径；
4. 无关系但不满足历史任务条件的请求不得进入legacy回退；
5. 新建任务API仍拒绝路径型`asset_id`；
6. 编码路径、绝对路径、Windows盘符、UNC、`..`、符号链接逃逸及允许根之外路径不能借legacy回退读取；
7. 重试、恢复和生产Worker入口遵守相同规则。

必须优先运行或构造生产装配级测试，不能只验证孤立Service单元测试。

## 第9节　D-02已登记输出与终态失败复验

必须故障注入验证：

1. 输出对象已发布且资产记录、任务关系已经提交后，任务成功终态CAS失败；
2. 租约在终态转换前丢失；
3. Worker恢复或重试再次观察同一输出；
4. 重复登记或重复恢复。

每个场景均需确认：

- 正式对象不被删除；
- `media_assets`记录保留且状态正确；
- `media_job_assets`关系保留；
- 任务输出兼容投影与关系一致；
- 恢复逻辑核验关系、资产状态和对象存在；
- 不重复创建逻辑资产或覆盖错误对象；
- 任务不会被错误标记为成功。

## 第10节　D-03登记提交结果分类复验

必须分别复现：

1. `not_committed`：事务确定未提交；
2. `committed`：调用方收到异常，但独立查询确认数据已提交；
3. `unknown`：无法可靠判断是否提交。

必须确认：

- 分类来自事务失败后的独立数据库查询和持久化事实，而非根据异常类型、异常文本或内存对象猜测；
- 只有可证明`not_committed`时才允许删除刚发布对象；
- `committed`时保留对象、资产记录和任务关系；
- `unknown`时不得猜测性删除对象；
- 后续恢复或重试能够安全对账，不会永久卡死、误报成功或创建重复资产；
- 查询本身失败时进入保守的`unknown`处理；
- 补偿和重试保持幂等。

报告必须列出每个故障注入点、持久化结果、对象结果和任务结果。

## 第11节　D-04物理清理并发与恢复复验

必须验证物理清理流程：

1. 通过条件更新取得清理权并先进入不可下载、不可新增引用的受控状态；
2. 新建任务引用、重试复制关系、输出关系登记和清理竞争同一资产状态约束；
3. 清理开始后无法新增任务关系；
4. 有既有引用时不能取得物理清理权；
5. 并发清理调用幂等；
6. 文件删除失败后状态可识别、可重试且不会伪装为正常可用资产；
7. 文件已删除但数据库最终删除失败时，资产不能恢复成可下载状态，后续重试可收敛；
8. 数据库记录已删除后的重复清理行为明确且安全；
9. 普通软删除语义和引用保护未被破坏。

不得仅根据代码锁或事务外观推断安全，必须用并发或确定性交错测试证明关键竞态。

## 第12节　D-05报告计数复验

必须核对：

- 首次02-D实施最终定向测试真实结果为`37 passed, 12 warnings`，而不是`35 passed`；
- 更正只修改首次实施报告中的错误口径；
- 没有把必要修复新增测试倒填到首次实施结果；
- 当前必要修复报告中的定向测试和最终测试计数可以由命令独立重现。

D-05本身不是代码通过条件，但报告若仍失真，应在复验结论中单列。

## 第13节　原02-D能力回归

除D-01至D-04外，必须确认首次验收已通过的关键能力没有回归：

- `media_assets`与`media_job_assets`模型、约束和关系；
- `0003`单一Alembic head及升降级；
- 上传即注册、UUID、幂等及失败补偿；
- 受控资产查询、下载、软删除和引用保护；
- Worker输入资产读取与输出登记；
- 新任务只接受真实资产UUID；
- 历史任务仅受控只读回退；
- 对账只报告且无破坏性副作用；
- 旧`/api/files`路径穿越、编码穿越、Windows路径、UNC、符号链接逃逸及错误脱敏；
- API响应不泄露`object_key`、Store根或绝对路径；
- 02-A、02-B、02-C主要协议和公开HTTP语义保持不变。

## 第14节　迁移复验

在隔离临时数据库中验证：

- `upgrade`到`0003`成功；
- `downgrade`回前一版本成功；
- 再次`upgrade`成功；
- 仍只有一个Alembic head；
- `0001`、`0002`、`0003`内容没有因必要修复变化；
- 往返只影响02-D授权对象，不损坏历史表；
- 无需外部数据库服务。

不得对用户正式数据库执行迁移。

## 第15节　测试完整性与算术

必须独立证明：

- 首次必要修复前基线：`270 passed`；
- 必要修复净新增：27项；
- 当前完整回归：`297 passed, 12 warnings`；
- 算术：`270 + 27 = 297`；
- 原270项没有删除、替换、改名逃避、跳过、`xfail`、排除、弱化断言或改成恒真；
- 本轮27项覆盖D-01至D-04，而非只增加无关测试；
- 测试收集结果不存在`skipped`、`xfail`或意外`deselected`。

如当前环境无法重现精确计数，必须说明原因、已取得的证据及影响，不得用“基本一致”代替。

## 第16节　必须执行的验证

在不违反只读边界的前提下，至少执行：

1. D-01至D-04定向测试；
2. 生产Worker装配与legacy隔离测试；
3. 输出登记、CAS、租约和提交未知故障注入测试；
4. 物理清理并发及失败恢复测试；
5. 02-D资产与安全回归组；
6. 全量离线测试；
7. Ruff只读检查；
8. `git diff --check`；
9. Alembic单一head和隔离往返；
10. 测试前后Git状态对比。

报告需记录准确命令、退出码、通过/失败/跳过/警告数量。命令中的本机绝对临时路径可适当脱敏，但不得省略关键参数。

## 第17节　禁止的通过方式

不得：

- 只引用必要修复报告中的测试结果；
- 只跑新增测试而不跑全量回归；
- 删除、跳过、`xfail`、排除或放宽断言；
- 用Mock完全绕过生产装配、数据库提交结果或对象存储行为；
- 用串行测试代替D-04并发竞争证明；
- 因最终显示`297 passed`而忽略真实一致性缺陷；
- 将“没有观察到失败”写成“已证明无竞态”；
- 在复验中修复问题后再判通过。

## 第18节　缺陷处理与结论规则

复验结论只能是以下四种之一：

1. **通过**：D-01至D-04全部以独立证据通过，D-05已正确更正，原能力与297项回归完整通过，无未解释差异；
2. **有条件通过**：仅存在不影响代码正确性、数据一致性、安全、兼容或提交范围的报告性小问题，且条件明确、无需代码修复；
3. **需要再次必要修复**：存在范围明确、可局部修复的代码、测试、迁移或一致性问题；
4. **存在阻断问题**：基线、Git状态、范围、数据一致性、安全或证据链存在重大问题，无法安全进入提交。

D-01至D-04任何一项未证实或失败，不得判“通过”或“有条件通过”。

## 第19节　复验报告必备结构

报告至少包括：

1. 结论摘要；
2. 独立性与只读声明；
3. 冻结基线和Git状态；
4. 逐文件差异审计表；
5. D-01至D-05逐项追踪矩阵；
6. D-01生产装配与legacy矩阵；
7. D-02/D-03失败点—数据库—文件—任务状态矩阵；
8. D-04并发交错与恢复矩阵；
9. 原02-D能力回归矩阵；
10. API和安全回归结果；
11. 迁移往返结果；
12. `270 + 27 = 297`测试完整性证据；
13. Ruff、diff检查和全部命令结果；
14. 测试前后Git状态对比；
15. 新发现问题、级别、证据和最小影响范围；
16. 四选一最终结论；
17. 是否允许进入“本地提交前审阅”的明确建议。

## 第20节　完成后状态

无论复验结果如何，完成后必须：

- 仅在仓库外生成复验报告；
- 保持仓库代码与测试完全不变；
- 保持暂存区为空；
- 保持HEAD不变；
- 不提交、不推送；
- 不启动再次修复；
- 不开始02-E；
- 立即停止并将报告交回主审阅聊天。

即使结论为“通过”，也只表示可以进入本地提交前审阅，不等于自动授权提交。

## 第21节　建议上传资料

新建独立复验聊天时，建议一次性上传以下8份资料：

1. 《阶段02-D素材与输出资产修复后独立只读复验任务.md》
2. 《阶段02-D素材与输出资产必要修复任务.md》
3. 《阶段02-D素材与输出资产必要修复报告.md》
4. 《阶段02-D素材与输出资产独立只读验收任务.md》
5. 《阶段02-D素材与输出资产独立只读验收报告.md》
6. 《阶段02-D素材与输出资产严格任务契约.md》
7. 《阶段02-D素材与输出资产实施报告.md》
8. 《阶段02-D素材与输出资产只读技术勘察报告.md》

如新聊天能够直接访问同一真实仓库，不需要重复上传代码、测试压缩包或02-A/02-B全部历史报告。若总体规划文件用于确认阶段边界，也可附加上传，但不得取代上述资料。

## 第22节　给独立复验Codex的开场指令

```text
请严格执行附件《阶段02-D素材与输出资产修复后独立只读复验任务.md》。

本次只授权你以新的独立Codex身份，对仓库D:\AI-Media-Platform\06-source\pixelle-video中尚未提交的阶段02-D首次实施及必要修复成果执行只读复验，并在仓库外生成《阶段02-D素材与输出资产修复后独立只读复验报告.md》。你不得修改或修复任何仓库文件。

开始前必须核验完整冻结HEAD 9ff23ffa262681df1df6ddc94d545cffede78677、父提交、分支、远程、跟踪关系、ahead/behind、暂存区、完整工作树、全部差异以及0001/0002/0003。当前预期是HEAD不变、暂存区为空、工作树包含未提交的02-D实施与必要修复成果。发现基线不符、已有新提交、迁移被越权修改或无法解释的文件变化，必须在测试前硬停止。

必须独立复验D-01至D-04：生产Worker真实装配下历史legacy回退可达且新任务无法利用；输出资产登记成功后终态CAS或租约失败不得删除正式对象；登记提交结果必须通过独立持久化查询区分not_committed、committed和unknown，unknown不得猜测性删除；物理清理必须先取得受控清理状态，并与新增引用、重试关系和输出登记竞争同一资产状态，且文件或数据库删除失败后可以安全重试收敛。同时核对D-05首次定向测试已从35正确更正为37 passed, 12 warnings。

必须复验首次02-D已通过的资产模型、上传与下载、软删除与引用保护、Worker资产链路、路径安全、legacy隔离、API脱敏及0003迁移。独立证明原270项测试完整保留、净新增27项、最终297 passed且没有删除、替换、跳过、xfail、deselected或弱化断言。运行定向故障注入、并发测试、02-D回归、全量离线测试、Ruff、git diff --check、Alembic单一head与隔离升降级，并比较测试前后Git状态。

不得联网、安装依赖、启动Uvicorn、正式数据库、Worker、GPU、ComfyUI、Provider或真实媒体任务；不得修改、格式化、自动修复、暂存、提交、推送、创建PR或开始02-E。任何问题只能记录，不能顺手修复。

最终结论只能是“通过”“有条件通过”“需要再次必要修复”或“存在阻断问题”之一。D-01至D-04任一项未被独立证实，不得判通过。完成报告后立即停止；即使通过，也不得自行创建本地提交。
```

## 第23节　建议聊天名称

> **AI Media Platform—阶段02-D素材与输出资产修复后独立只读复验**

## 第24节　最终硬停止条件

出现以下任一情况时，不得给出通过结论：

- D-01至D-04任一缺陷仍可复现或证据不足；
- 生产装配行为与孤立单元测试不一致；
- `unknown`提交结果仍可能误删正式对象；
- 清理状态不能阻止并发新增引用；
- 新任务能够利用legacy路径回退；
- 原270项测试有减少、替换、跳过或弱化；
- 297项全量回归失败；
- 迁移不再是单一head或往返破坏历史结构；
- 出现路径逃逸、敏感路径泄露或公共协议回归；
- Git差异超出报告和授权范围；
- 测试后仓库产生新的未解释变化；
- 必须修改代码才能继续复验。

此时应保留全部证据，在仓库外完成报告并停止。
