# AI Media Platform—阶段02代表性端到端可行性验证严格实施任务

版本日期：2026-07-29
任务性质：阶段02真实运行验证；允许启动本地API、独立Worker、既有ComfyUI Provider和一次受控4090真实生成
目标仓库：`D:\AI-Media-Platform\06-source\pixelle-video`
预期分支：`phase2-persistent-media-jobs`
冻结HEAD：`35a6705f8f309d7af8853ea201fa991a3c03f730`

---

## 0. 任务结论先行

阶段02-E已经通过02-E2-B路径A正式关闭。当前不进入阶段03，也不继续迁移旧复合入口。

本任务只验证以下最短真实生产链是否能够跑通：

```text
输入资产
→ POST /api/media/jobs
→ 唯一SQLite任务
→ 独立Worker唯一领取
→ 既有4090 ComfyUI工作流唯一提交
→ prompt_id持久化
→ 完成状态持久化
→ 输出资产登记
→ 通过资产接口查询或下载结果
```

同时执行一个不消耗GPU的排队任务取消场景，证明取消后的任务不会提交Provider。

本任务需要真实GPU，但**不得立即开启GPU服务器**。必须先完成第5节全部无GPU预检；预检通过后，Codex明确输出“请开启4090服务器”的暂停点，用户确认服务器已开启后，才进入真实运行阶段。真实生成及必要证据采集结束后立即通知用户关闭4090服务器。

---

## 1. 新对话及输入文件

### 1.1 新建Codex对话

必须新建独立Codex实施对话，不继续使用02-E2-B实施对话。

对话名称：

```text
AI Media Platform—阶段02代表性端到端可行性验证
```

当前ChatGPT统筹与验收对话可以继续使用，不需要另开新的ChatGPT对话。

### 1.2 新Codex对话上传文件

上传以下6份文件：

1. `AI-Media-Platform-总规划与实施基线-V2.3.md`
2. `阶段02-E旧入口统一迁移严格实施任务契约.md`
3. `阶段02-E2-A推送个人Fork重试报告.md`
4. `阶段02-E2-B新对话交接基线与快速推进原则.md`
5. `阶段02-E2-B快速闭环实施报告.md`
6. `阶段02代表性端到端可行性验证严格实施任务.md`

不需要上传02-E1或02-E2-A历次修复、补正、复验文件，也不需要上传02-E2-B已经执行完毕的严格任务文件。

---

## 2. 权威状态

开始时必须接受并核验：

| 项目 | 权威值 |
| --- | --- |
| 仓库 | `D:\AI-Media-Platform\06-source\pixelle-video` |
| 分支 | `phase2-persistent-media-jobs` |
| HEAD | `35a6705f8f309d7af8853ea201fa991a3c03f730` |
| origin目标分支 | `origin/phase2-persistent-media-jobs` |
| 远端SHA | `35a6705f8f309d7af8853ea201fa991a3c03f730` |
| behind / ahead | `0 / 0` |
| 起点工作树 | 干净 |
| 起点暂存区 | 空 |
| 起点未跟踪文件 | 无 |
| 02-E状态 | 已完成并关闭 |
| 下一阶段 | 本任务；不是阶段03 |

总规划V2.3中的动态状态早于02-E2-A、02-E2-B。若其动态状态与本表冲突，以本任务、02-E2-B最终实施报告和实时Git核验结果为准；长期架构与治理原则仍然有效。

---

## 3. 快速推进原则

1. 第一目标是尽快证明真实代表性链路可行。
2. 不重复02-E2-A的47项探针、98项架构测试或434项全量测试。
3. 不围绕生产代码未使用的理论AST边界、冷门语法或明确延期入口继续扩测。
4. 无代码改动时只运行本任务必需的定向检查。
5. 只有真实生产链失败、重复Provider提交、双任务ID、SQLite事实源分裂、接口合同破坏、资产不一致、取消后仍提交Provider、Git现场漂移，才构成实质阻断。
6. 网络瞬时错误、首次冷启动、可解释的非阻塞warning不得直接判定架构失败；允许一次受控重试，并必须记录。
7. 不以“补齐形式”为理由扩建功能、迁移旧复合入口或进入阶段03。

---

## 4. 起点Git硬门禁

在启动任何服务或创建运行数据前，执行一次只读核验：

```powershell
Set-Location "D:\AI-Media-Platform\06-source\pixelle-video"
git rev-parse --show-toplevel
git branch --show-current
git rev-parse HEAD
git status --short
git diff --name-only
git diff --cached --name-only
git remote -v
git fetch origin phase2-persistent-media-jobs
git rev-parse origin/phase2-persistent-media-jobs
git rev-list --left-right --count origin/phase2-persistent-media-jobs...HEAD
git show -s --format="%H%n%P%n%s" HEAD
```

必须确认：

- 仓库、分支、HEAD、origin和远端SHA与第2节一致；
- behind / ahead为`0 / 0`；
- 工作树干净；
- 暂存区为空；
- 无未跟踪文件；
- HEAD父提交和主题与02-E2-A推送报告一致。

出现以下任一情况立即停止并报告，不得自动清理、切分支、回退、stash、pull、merge或reset：

- HEAD、分支、父提交或提交主题不符；
- origin指向不符；
- 实时远端SHA不等于冻结HEAD；
- behind / ahead不是`0 / 0`；
- 工作树、暂存区或未跟踪文件不为空；
- 无法完整读取上传文件或本地仓库。

---

## 5. 无GPU预检

本节全部通过前，禁止要求用户开启4090或A800。

### 5.1 读取真实启动和配置方式

从当前仓库真实代码、现有说明和既有配置中确认并在报告记录：

- API的真实启动命令、监听地址和健康检查；
- 独立Worker的真实启动命令；
- SQLite数据库配置项；
- 素材根目录和输出根目录配置项；
- ComfyUI Provider配置项；
- `POST /api/media/jobs`的真实请求合同；
- 输入资产上传或注册接口；
- 任务查询、取消、资产查询和资产下载接口；
- 当前已注册四工作流及其真实工作流标识；
- 4090上已经验证过的I2V工作流JSON和最小输入素材位置。

优先检查既有目录：

```text
D:\AI-Media-Platform\02-workflows\4090
```

不得在报告中输出Token、密钥、完整私有服务地址或其他秘密。只记录脱敏配置来源和是否可用。

### 5.2 选择代表性工作流

优先选择4090上已有成功记录、能被当前`MediaJobRequest`和工作流注册表无损表达的最短I2V工作流。

若`I2V_480p_Cat`仍是当前注册表中的有效标识，优先使用该工作流及其已有最小有效素材；若真实注册表名称不同，以真实代码和现有工作流文件为准，并在报告解释选择。

不得为了适配测试而新增工作流、修改ComfyUI节点、下载模型、安装依赖或切换到A800。

### 5.3 隔离运行数据

不得覆盖现有开发数据库、用户素材或历史输出。为本次验证建立独立的仓库外运行目录，例如：

```text
D:\AI-Media-Platform\90-exports\phase02-e2e-YYYYMMDD-HHMMSS\
```

该目录至少分为：

```text
db\
assets\
outputs\
logs\
evidence\
```

所有本轮SQLite、上传素材副本、输出、服务日志和脱敏证据写入该目录。不得把二进制媒体、数据库、原始日志或秘密写入Git仓库。

### 5.4 静态与离线定向门禁

只运行与真实链路直接相关的最小测试集合：

- 持久化任务API创建/查询/取消合同；
- Worker领取与单提交；
- `prompt_id`持久化与完成协调；
- 输入/输出资产；
- 02-E2-A单提交和Facade收口测试。

由Codex从当前仓库真实测试名选择，不得为了数量重复全量回归。记录每个实际测试文件/节点和真实结果。

同时执行：

```text
git diff --check
Alembic单一head检查
0001—0003迁移相对冻结HEAD零差异检查
```

无代码改动时，不运行434项全量测试。

### 5.5 本地无GPU取消场景

先验证取消场景，避免占用GPU：

1. 使用隔离数据库和资产目录启动API，但暂不启动Worker；
2. 通过真实资产接口上传或注册一个合法输入资产；
3. 通过`POST /api/media/jobs`创建一个合法持久化任务；
4. 记录唯一`job_id`；
5. 在Worker未启动时通过公开取消接口取消任务；
6. 启动Worker并至少等待两个真实轮询周期；
7. 证明该任务仍为取消终态；
8. 证明没有`prompt_id`；
9. 证明没有Provider提交；
10. 证明没有输出资产；
11. 停止本地服务，保留脱敏证据。

若现有公开合同要求先启动Worker才能完成API初始化，可按真实实现调整进程顺序，但必须通过可控暂停、队列隔离或等价机制保证取消发生在Provider提交前；不得修改生产代码只为制造该场景。

### 5.6 GPU启动暂停点

只有第4节和第5.1—5.5节全部通过，Codex才可以暂停并向用户明确输出：

```text
无GPU预检已经通过。现在请开启4090服务器；不要开启A800。4090启动完成并确认ComfyUI可用后，请回复“4090已开启”。
```

暂停时同时列出：

- 已通过的预检；
- 将使用的工作流标识；
- 本次预计只提交一个真实生成任务；
- 不得让用户同时开启A800；
- 继续后将先做只读连通性检查，不会直接盲目提交。

---

## 6. 4090真实端到端验证

用户明确回复“4090已开启”后，才执行本节。

### 6.1 连通性与安全检查

1. 只读检查4090 ComfyUI健康状态和所需工作流能力；
2. 不打印真实Token或完整私有地址；
3. 不修改GPU服务器模型、节点或系统环境；
4. 不安装依赖；
5. 不上传与本任务无关的数据；
6. 若健康检查失败，允许一次短时受控重试；
7. 两次均失败则记录环境阻断并立即要求用户关闭4090。

### 6.2 启动本地真实链

使用隔离运行目录和真实项目启动方式：

1. 启动API；
2. 启动一个且仅一个独立Worker进程；
3. 确认二者使用同一个隔离SQLite数据库；
4. 确认Worker使用4090既有ComfyUI Provider；
5. 确认没有第二个Worker或旧TaskManager并行消费本任务；
6. 记录启动时间、进程角色和脱敏配置摘要。

### 6.3 创建唯一真实任务

1. 通过真实资产接口上传或注册最小有效I2V输入素材；
2. 保存返回的唯一输入`asset_id`；
3. 只调用一次`POST /api/media/jobs`；
4. 保存HTTP状态、响应合同和唯一`job_id`；
5. 不调用旧同步、旧async、Library直调或Streamlit旁路；
6. 不因轮询或客户端重试再次提交创建请求；
7. 不创建第二个成功路径任务。

### 6.4 任务执行和单提交证据

持续观察直到成功终态或真实失败终态，记录：

- 任务状态转换；
- Worker领取次数；
- Provider提交次数；
- ComfyUI返回的唯一`prompt_id`；
- `prompt_id`写入SQLite的证据；
- 轮询或恢复期间没有再次提交；
- 任务最终状态；
- 失败时的脱敏错误类型和最小复现证据。

成功判定必须同时满足：

```text
一个job_id
一个SQLite任务事实源
一个Worker有效领取
一次Provider提交
一个prompt_id
一个成功终态
```

### 6.5 输出资产闭环

成功后必须验证：

1. 输出资产记录存在；
2. 输出资产关联到同一个`job_id`；
3. 输出相对路径满足资产安全规则；
4. 输出文件真实存在且大小大于0；
5. 通过公开资产查询或下载接口能够取得结果；
6. 记录文件类型、字节数和SHA-256；
7. 不把二进制视频加入Git；
8. 不把真实私有地址写入报告。

### 6.6 立即关闭GPU

完成以下任一情况后立即要求用户关闭4090：

- 成功路径全部证据已取得；
- 已出现不可继续的真实链路失败；
- Provider或ComfyUI连续两次不可用；
- 发现重复提交风险；
- 发现输出已经产生但后续只是离线整理报告；
- Codex需要开始代码修复。

Codex必须明确输出：

```text
真实GPU步骤已经结束。现在请立即关闭4090服务器，避免继续计费；不需要开启A800。
```

用户确认关闭前，Codex可以整理已经取得的离线证据，但不得再提交GPU任务。

---

## 7. 失败与最小修复规则

### 7.1 环境或配置阻断

允许修正仅属于本次隔离运行目录的可逆配置，例如：

- 隔离数据库路径；
- 隔离素材/输出目录；
- 进程启动参数；
- 已存在Provider配置的正确引用；
- 端口冲突的本地替代端口。

不得修改或泄露秘密，不得改动GPU服务器环境，不得安装依赖。

修正后允许一次重试。仍失败则报告并停止。

### 7.2 真实代码缺陷

若真实链暴露阶段02范围内的单一、明确、可最小修复的代码缺陷，本任务预授权一次窄范围修复循环，但必须同时满足：

- 缺陷直接阻断第6节真实链；
- 不新增数据库迁移；
- 不改变公开HTTP合同；
- 不进入旧复合入口或阶段03；
- 不削弱安全校验；
- 不引入新依赖；
- 不修改GPU服务器；
- 修复范围可以被定向测试覆盖；
- 不创建提交、不推送、不创建PR。

修复前必须：

1. 记录失败证据；
2. 关闭4090；
3. 写明根因和预计修改文件；
4. 确认工作树除本任务报告外没有其他意外变化。

修复后：

1. 运行相关定向测试；
2. 运行`git diff --check`；
3. 重新完成第5节必要预检；
4. 再次要求用户手动开启4090；
5. 只允许再提交一个真实生成任务；
6. 结束后立即关闭4090。

若存在两个以上独立代码缺陷，或需要迁移、接口重设计、数据库迁移、新依赖、阶段03能力，立即停止并输出阻断报告，不得继续扩展。

### 7.3 重复提交硬停止

一旦发现同一`job_id`出现两个`prompt_id`、两次Provider提交、两个任务事实源、两个成功输出集合，立即停止：

- 不再重试；
- 不再创建任务；
- 立即要求关闭4090；
- 保存脱敏证据；
- 不自动修复；
- 报告标记为数据一致性硬阻断。

---

## 8. 明确禁止事项

- 未通过无GPU预检就开启GPU；
- 同时开启4090和A800；
- 使用A800作为默认回退；
- 运行超过本任务所需的一次成功任务和最多一次修复后重试任务；
- 运行旧同步、旧async、Library或Streamlit生产旁路；
- 开始阶段03；
- 扩建复合编排；
- 新增数据库迁移；
- 下载模型、安装依赖或修改ComfyUI环境；
- 为理论边界新增测试；
- 运行434项全量测试作为无代码改动任务的形式门禁；
- 将数据库、视频、素材副本、原始日志或秘密加入Git；
- 暂存、提交、推送、创建PR、merge或发布；
- 自动清理、stash、reset或删除用户文件。

---

## 9. 交付物与保存目录

### 9.1 本任务文件

将本任务文件下载并保存到：

```text
D:\AI-Media-Platform\06-source\pixelle-video\docs\zh\development\阶段02代表性端到端可行性验证严格实施任务.md
```

### 9.2 正式实施报告

Codex完成后生成：

```text
D:\AI-Media-Platform\06-source\pixelle-video\docs\zh\development\阶段02代表性端到端可行性验证实施报告.md
```

报告至少包含：

1. 最终结论：通过、通过但有最小修复、环境阻断或真实代码阻断；
2. 冻结起点及实时Git核验；
3. 实际读取的启动、配置、API、Worker、Provider和工作流事实；
4. 选定工作流和输入素材；
5. 隔离运行目录；
6. 无GPU预检命令及真实结果；
7. 取消场景的`job_id`、状态、无`prompt_id`、无Provider提交和无输出资产证据；
8. 4090开启和关闭时间；
9. 成功路径唯一`asset_id`、`job_id`和`prompt_id`的脱敏证据；
10. Worker领取次数和Provider提交次数；
11. 任务状态转换；
12. 输出资产ID、相对路径、类型、字节数和SHA-256；
13. 资产查询或下载接口结果；
14. 所有受控重试；
15. 若有修复：根因、修改文件、diff摘要和定向测试；
16. 未执行事项；
17. 最终Git现场；
18. 是否建议进入阶段02收尾验收，或需要单独修复任务。

报告不得包含：

- Token、密钥、Authorization头；
- 完整私有服务地址；
- 用户个人信息；
- 原始大段服务日志；
- 对未运行测试的“通过”声明；
- “整个平台已可生产部署”等越界结论。

### 9.3 运行证据

数据库、素材副本、输出视频、日志和证据保存在本轮独立目录：

```text
D:\AI-Media-Platform\90-exports\phase02-e2e-YYYYMMDD-HHMMSS\
```

实施报告只引用脱敏摘要和本地相对证据路径，不把这些运行产物复制到源码仓库。

---

## 10. 最终Git现场

结束前执行：

```powershell
Set-Location "D:\AI-Media-Platform\06-source\pixelle-video"
git rev-parse HEAD
git status --short
git diff --name-only
git diff --cached --name-only
git rev-list --left-right --count origin/phase2-persistent-media-jobs...HEAD
git diff --check
```

预期：

- HEAD仍为`35a6705f8f309d7af8853ea201fa991a3c03f730`；
- behind / ahead仍为`0 / 0`；
- 暂存区为空；
- 未提交、未推送、未创建PR；
- 若无代码修复，仓库变化只能是本任务文件和实施报告；
- 若发生一次获准最小修复，必须逐项列出代码、测试和报告变化；
- 运行数据库、日志、素材和视频不得出现在Git状态中。

若出现其他意外变化，停止并报告，不得自动清理。

---

## 11. 给新Codex的完整开场白

将第1.2节6份文件上传到新Codex对话后，发送以下完整文字：

```text
对话名称：AI Media Platform—阶段02代表性端到端可行性验证

这是AI Media Platform项目阶段02的真实端到端验证任务。

请先完整阅读我上传的6份文件，再执行任何仓库操作。当前权威状态如下：

1. 阶段02-E2-B已经按路径A验收通过，阶段02-E已经正式关闭。
2. 当前分支为phase2-persistent-media-jobs。
3. 冻结HEAD为35a6705f8f309d7af8853ea201fa991a3c03f730。
4. origin同名分支实时基线应与冻结HEAD一致，behind 0 / ahead 0，起点工作树干净。
5. 本任务是“阶段02代表性端到端可行性验证”，不是阶段03，也不继续迁移旧复合入口。
6. 本任务优先尽快证明真实调用链可行，只允许真实生产链失败、重复提交、双任务ID、事实源分裂、接口合同破坏、资产不一致、取消后仍提交Provider或Git现场漂移构成阻断。
7. 不重复02-E2-A的47项探针、98项架构测试或434项全量测试。

严格按照《阶段02代表性端到端可行性验证严格实施任务.md》执行。

先完成Git只读硬门禁和全部无GPU预检。此时不要让我开启GPU，也不要自行启动计费GPU资源。无GPU预检必须包括：真实启动与配置核对、隔离运行目录、最小定向测试、Alembic与迁移零差异检查，以及一个Worker提交前取消且不触发Provider的排队任务场景。

全部无GPU预检通过后，请暂停并明确告诉我“请开启4090服务器”。不要开启A800。等我回复“4090已开启”后，先做只读健康检查，再只创建一个POST /api/media/jobs真实I2V任务，验证唯一job_id、唯一Worker领取、一次Provider提交、唯一prompt_id、SQLite状态持久化、成功终态、输出资产登记以及通过资产接口查询或下载结果。

真实GPU步骤结束后，无论成功或阻断，都必须立即告诉我关闭4090服务器。未经本任务规则允许，不得创建第二个任务或继续消耗GPU。

如发现一个直接阻断真实链、且满足任务第7.2节全部条件的单一代码缺陷，可以在关闭4090后进行一次最小修复循环；不得提交、推送、创建PR，不得新增迁移、新依赖、改变公开合同或进入阶段03。出现重复Provider提交、双prompt_id或事实源分裂时必须硬停止，不得自动修复。

正式报告保存到：
D:\AI-Media-Platform\06-source\pixelle-video\docs\zh\development\阶段02代表性端到端可行性验证实施报告.md

数据库、素材副本、输出、日志和脱敏证据保存在：
D:\AI-Media-Platform\90-exports\phase02-e2e-YYYYMMDD-HHMMSS\

未经明确授权，不暂存、不提交、不推送、不创建PR，不开始阶段03，不修改GPU服务器环境，不安装依赖。

现在先完整复述你理解的任务边界，然后只执行Git起点硬门禁和无GPU预检。到达GPU启动暂停点后停止，等我确认4090已开启。
```

---

## 12. 完成判定

### 12.1 通过

必须同时满足：

- 起点和终点Git现场可解释；
- 无GPU取消场景通过；
- 真实成功路径只有一个`job_id`；
- 只有一次Worker有效领取；
- 只有一次Provider提交；
- 只有一个`prompt_id`并已持久化；
- 任务达到成功终态；
- 输出资产真实登记并可由公开接口查询或下载；
- 输出文件大小大于0且SHA-256已记录；
- GPU已关闭；
- 正式报告和脱敏证据已落盘；
- 未提交、未推送、未创建PR。

### 12.2 通过但有最小修复

除满足12.1外，还必须：

- 只有一个真实代码缺陷；
- 修复符合第7.2节；
- 定向测试通过；
- 修复后真实链重新通过；
- 所有修改保持本地未提交，等待外部审阅。

### 12.3 阻断

以下任一情况均应保存证据、关闭GPU并停止：

- 无法通过冻结起点硬门禁；
- 两次受控连通性检查均失败；
- 两次环境配置尝试仍不能启动真实链；
- 出现重复Provider提交、双`prompt_id`、双任务ID或事实源分裂；
- 输出资产与任务状态不一致；
- 取消后仍提交Provider；
- 需要数据库迁移、公开合同改变、新依赖或阶段03能力；
- 存在两个以上独立真实代码缺陷；
- 出现意外Git现场变化。

本任务完成后，Codex必须停止并将正式报告交回当前ChatGPT统筹与验收对话。未经外部审阅，不得自行创建提交、推送个人Fork或进入下一阶段。
